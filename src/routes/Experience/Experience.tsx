import * as React from 'react';
import { PageHeader } from '../../common';
import { ExList } from '../../containers';

export const Experience = () => (
  <>
    <PageHeader title="Experience" />
    <ExList />
  </>
);
